from django import template
# from ..models import Booking
#
# register = template.Library()
#
#
# @register.filter
# def bookings(user, event):
#     try:
#         return Booking.objects.get(user=user, event=event)
#     except Booking.DoesNotExist:
#         return None
